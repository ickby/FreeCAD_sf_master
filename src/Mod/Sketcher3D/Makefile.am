SUBDIRS=App Gui

# Change data dir from default ($(prefix)/share) to $(prefix)
datadir = $(prefix)/Mod/Sketcher3D
data_DATA = Init.py InitGui.py

EXTRA_DIST = \
		$(data_DATA) \
		CMakeLists.txt \
		Sketcher3D.dox
